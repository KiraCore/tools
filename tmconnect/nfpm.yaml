name: "tmconnect"
arch: "${ARCH}"
platform: "${PLATFORM}"
version: "${VERSION}"
section: "default"
priority: "extra"
replaces:
- tmconnect
provides:
- tmconnect
maintainer: "KIRA <partners@kira.global>"
description: "TMCONNECT is KIRA a tool enabling to establish connection and handshake with tendermint nodes"
homepage: "https://github.com/KiraCore/tools"
license: "AGPL-3.0 license"
contents:
- src: ${SOURCE}
  dst: ./bin/
