name: "ipfs-api"
arch: "${ARCH}"
platform: "${PLATFORM}"
version: "${VERSION}"
section: "default"
priority: "extra"
replaces:
- ipfs-api
provides:
- ipfs-api
maintainer: "KIRA <partners@kira.global>"
description: "IPFS-API is a kira cli tool to manipulate data whithin IPFS using pinata.cloud as node service provider"
homepage: "https://github.com/KiraCore/tools"
license: "AGPL-3.0 license"
contents:
- src: ${SOURCE}
  dst: ./bin/
